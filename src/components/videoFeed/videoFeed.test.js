import React from 'react';
import ReactDOM from 'react-dom';
import VideoFeed from './index.js';
import { shallow } from 'enzyme';

xit('mounts the video feed', () => {
  expect(mediaDevices.getUserMedia.calledOnce).toBe(true)
});
